
// Enum Validation for the Email Address
enum EmailValidType{
  EMAIL_VALID,
  EMAIL_INVALID,
  EMAIL_NOT_SET,
  EMAIL_EXISTS
}

enum Validations{
  EMPTY,
  NONE,
  SUCCESS,
  SAME,
  FAILED,
  FORMAT
}

enum TextFields{
  EMAIL,
  PASSWORD,
  CONFIRM_PASSWORD
}

enum LoginState{
  LOGGED,
  NOT_LOGGED
}

