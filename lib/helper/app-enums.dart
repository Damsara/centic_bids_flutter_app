
// Enum Validation for the Email Address
enum EmailValidType{
  EMAIL_VALID,
  EMAIL_INVALID,
  EMAIL_NOT_SET,
  EMAIL_EXISTS
}

enum Validations{
  EMPTY,
  NONE,
  SUCCESS,
  FAILED
}

enum TextFields{
  EMAIL,
  PASSWORD,
  CONFIRM_PASSWORD
}

